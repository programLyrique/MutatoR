% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mutator.R
\name{mutate_package}
\alias{mutate_package}
\title{Run Mutation Testing for an R Package}
\usage{
mutate_package(
  pkg_dir,
  cores = max(1, parallel::detectCores() - 2),
  isFullLog = FALSE,
  detectEqMutants = FALSE,
  mutation_dir = NULL,
  max_mutants = NULL,
  timeout_seconds = NULL,
  config_dir = getwd(),
  max_line_deletions = 5,
  cran = TRUE,
  fail_fast = TRUE,
  strategy = c("auto", "testthat", "installed")
)
}
\arguments{
\item{pkg_dir}{Path to the package directory.}

\item{cores}{Number of parallel workers used for mutant test execution.}

\item{isFullLog}{Logical; if \code{TRUE}, prints per-mutant logs and timeout info.}

\item{detectEqMutants}{Logical; if \code{TRUE}, survived mutants are analyzed for
equivalence using the OpenAI-based workflow.}

\item{mutation_dir}{Optional directory to store generated mutant files.
If \code{NULL}, a temporary directory is used.}

\item{max_mutants}{Optional cap on the number of mutants tested.}

\item{timeout_seconds}{Optional timeout in seconds for each mutant run.
If \code{NULL}, timeout is derived from baseline runtime with a small minimum
floor. Each mutant's tests run in a separate subprocess, so the limit is
enforced as a hard wall-clock kill even when a mutant loops inside compiled
code (via \pkg{callr} for the \code{testthat} strategy and
\code{system2(timeout=)} for the installed-tests strategy).}

\item{config_dir}{Directory searched for a \code{.openai_config} file when
\code{detectEqMutants = TRUE} (see \code{\link[=get_openai_config]{get_openai_config()}}). Defaults to the
current working directory.}

\item{max_line_deletions}{Maximum number of line-deletion mutants per \code{.R}
file (passed to \code{\link[=mutate_file]{mutate_file()}}); \code{0} disables them. Defaults to \code{5}.}

\item{cran}{Logical; if \code{TRUE} (the default), tests run in "CRAN mode": the
\code{NOT_CRAN} environment variable is set to \code{"false"} in the test subprocess
so \code{testthat::skip_on_cran()} / \code{skip_if_offline()} guards take effect and
the same tests CRAN would run are used (skipping network/slow tests the
package marks). Set to \code{FALSE} to run the full suite (\code{NOT_CRAN = "true"}),
as \code{devtools::test()} does. Note this only affects tests the package
actually guards; unguarded network tests still run.}

\item{fail_fast}{Logical; if \code{TRUE} (the default), a mutant's test run stops
at the first failing test rather than running the whole suite. A mutant is
\code{KILLED} as soon as one test detects it, so the remainder of the suite is
wasted work; stopping early speeds up the test-running phase without
changing any mutant's verdict. Set to \code{FALSE} to run the full suite for
every mutant. Applies to the \code{testthat} strategy; the installed-tests
fallback already stops at the first failing test file regardless of this
flag.}

\item{strategy}{Test strategy to use. \code{"auto"} (the default) picks the
\code{testthat} strategy when \verb{tests/testthat/} exists and the installed-tests
strategy otherwise. \code{"testthat"} forces the in-process \code{testthat::test_dir()}
path (requires \verb{tests/testthat/}). \code{"installed"} forces the
\verb{R CMD INSTALL --install-tests} + \code{tools::testInstalledPackage()} path
(requires \verb{tests/}); this works for \code{testthat} packages too — useful for
comparing the two strategies' run times — but is slower because each mutant
must be installed before its tests run.}
}
\value{
An invisible list with three components:
\describe{
\item{\code{package_mutants}}{Named list with mutant path, mutation info, status,
and optional equivalence flags.}
\item{\code{test_results}}{Named list mapping mutant IDs to statuses:
\code{"KILLED"}, \code{"SURVIVED"}, or \code{"HANG"}.}
\item{\code{timing}}{Named list of phase durations in seconds: \code{baseline},
\code{generation}, \code{test_execution}, and \code{equivalence_detection}.}
}
}
\description{
Mutates all \code{.R} files under a package's \verb{R/} directory, runs the package's
tests against each mutant in parallel, and summarizes mutation outcomes.
}
\details{
Test strategy is, by default, detected automatically:
\itemize{
\item If \verb{tests/testthat/} exists, the mutant is loaded in-process with
\code{pkgload::load_all()} (no installation) and its tests are run the way the
package's own \code{tests/testthat.R} harness runs them -- i.e. with the same
arguments (notably any \code{filter}) that the harness passes to
\code{testthat::test_check()} -- via \code{testthat::test_dir()}.
\item Otherwise, if \verb{tests/} exists, mutator installs the mutant package
with \code{--install-tests} and runs \code{tools::testInstalledPackage()}.
}
Pass \code{strategy} to override this (for example to run a \code{testthat} package
through the slower installed-tests path for comparison).
}
\examples{
# Wrapped in \donttest{}: it loads and test-runs a throwaway package, which
# is too slow/heavy for routine automated checks.
\donttest{
pkg <- file.path(tempdir(), "examplepkg")
dir.create(file.path(pkg, "R"), recursive = TRUE, showWarnings = FALSE)
dir.create(file.path(pkg, "tests", "testthat"), recursive = TRUE, showWarnings = FALSE)
writeLines(c(
  "Package: examplepkg",
  "Title: Example Package",
  "Version: 0.0.1",
  "Description: Minimal package for a mutator example.",
  "License: GPL-3",
  "Encoding: UTF-8"
), file.path(pkg, "DESCRIPTION"))
writeLines("export(add)", file.path(pkg, "NAMESPACE"))
writeLines("add <- function(x, y) x + y", file.path(pkg, "R", "add.R"))
writeLines(
  "testthat::expect_equal(add(1, 2), 3)",
  file.path(pkg, "tests", "testthat", "test-add.R")
)
result <- mutate_package(pkg, cores = 1, max_mutants = 1, timeout_seconds = 10)
names(result)
}

}
