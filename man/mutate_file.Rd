% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mutator.R
\name{mutate_file}
\alias{mutate_file}
\title{Generate Mutants for a Single R File}
\usage{
mutate_file(
  src_file,
  out_dir = "mutations",
  max_mutants = NULL,
  max_line_deletions = 5
)
}
\arguments{
\item{src_file}{Path to an R source file.}

\item{out_dir}{Directory where mutant files are written.}

\item{max_mutants}{Optional cap on the number of returned mutants. If set,
a random subset of generated mutants is returned.}

\item{max_line_deletions}{Maximum number of line-deletion mutants generated
per file (a random subset of deletable lines). These complement the
AST-based statement deletions by also covering top-level / non-block lines.
Use \code{0} to disable line-deletion mutants entirely. Defaults to \code{5}.}
}
\value{
A list of mutants. Each element contains:
\describe{
\item{\code{path}}{Path to the mutant file.}
\item{\code{info}}{Formatted mutation metadata (file, source range, and details).}
\item{\code{loc}}{Machine-readable location: a list with \code{file_path},
\code{start_line}, and \code{end_line} (the latter two \code{NA} when unavailable).}
}
}
\description{
Creates mutants for a single R source file by combining AST-based mutations
from the C++ mutation engine with fallback line-deletion mutants.
}
\examples{
src <- tempfile(fileext = ".R")
writeLines("add <- function(x, y) x + y", src)
mutants <- mutate_file(src, out_dir = tempfile("mutations_"), max_mutants = 1)
length(mutants)

}
